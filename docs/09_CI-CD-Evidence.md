# CI/CD Evidence

## Pipeline Configuration
- Workflow: `.github/workflows/ci.yml`

## Successful Run Evidence
- [ ] Add screenshot of a successful run (Actions tab)

## Failed Run Evidence (optional but useful)
- [ ] Add screenshot/log of a failed run (e.g., from a temporary test failure)

## Notes
This pipeline runs:
- `npm ci`
- `npm run lint`
- `npm test`
- `npm run build`
